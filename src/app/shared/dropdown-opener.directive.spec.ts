import { DropdownOpenerDirective } from './dropdown-opener.directive';

describe('DropdownOpenerDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownOpenerDirective();
    expect(directive).toBeTruthy();
  });
});
